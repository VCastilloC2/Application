# Application
API REST desarrollada con Spring Boot que implementa creación y consulta de usuarios utilizando base de datos y arquitectura desacoplada (Controller, Service, DTO y Entity). Incluye autenticación con Spring Security, control de acceso a endpoints y manejo de parámetros por URL. El servidor está configurado en el puerto 9000.
